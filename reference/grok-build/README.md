# Grok Reference Build

Place the clean exported Grok-derived build in this directory.

## Intake rules

- Preserve the export unchanged where possible.
- Remove secrets, API keys, tokens, and private credentials.
- Record the source, export date, version, and provenance in `PROVENANCE.md`.
- Do not assume opaque hosted services are locally implemented.
